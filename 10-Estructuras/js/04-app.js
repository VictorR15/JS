const dinero = 300;
const totalPagar = 300;
const tarjeta = true;

if (dinero >= totalPagar) {
  console.log("Se puede pagar");
} else if (tarjeta) {
  console.log("Se puede pagar");
} else {
  console.log("No se puede pagar");
}
