//ACEDER A ELEMENTOS DEL HTML
let elemeto;

elemeto = document;
elemeto = document.all;
elemeto = document.head;
elemeto = document.body;
elemeto = document.domain;
elemeto = document.forms;
elemeto = document.forms[0];
elemeto = document.forms[0].id;
elemeto = document.forms[0].method;
elemeto = document.forms[0].classList;
elemeto = document.forms[0].action;

elemeto = document.links;
elemeto = document.links[4];
elemeto = document.links[4].classList;
elemeto = document.links[4].className;

elemeto = document.images;

elemeto = document.scripts;

console.log(elemeto);
